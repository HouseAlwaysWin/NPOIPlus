name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  build:
    name: Build and Test
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: |
          6.0.x
          7.0.x
          8.0.x
    
    - name: Display .NET info
      run: dotnet --info
    
    - name: Restore dependencies
      run: |
        dotnet restore FluentNPOI/FluentNPOI.csproj
        dotnet restore FluentNPOIConsoleExample/FluentNPOIConsoleExample.csproj
        dotnet restore FluentNPOIUnitTest/FluentNPOIUnitTest.csproj
    
    - name: Build FluentNPOI
      run: dotnet build FluentNPOI/FluentNPOI.csproj --configuration Release --no-restore
    
    - name: Build Console Example
      run: dotnet build FluentNPOIConsoleExample/FluentNPOIConsoleExample.csproj --configuration Release --no-restore
    
    - name: Build Unit Tests
      run: dotnet build FluentNPOIUnitTest/FluentNPOIUnitTest.csproj --configuration Release --no-restore
    
    - name: Run Unit Tests
      run: dotnet test FluentNPOIUnitTest/FluentNPOIUnitTest.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx" --results-directory ./TestResults
    
    - name: Publish Test Results
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Test Results
        path: 'TestResults/*.trx'
        reporter: dotnet-trx
    
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v6
      with:
        name: build-artifacts
        path: |
          FluentNPOI/bin/Release/
          FluentNPOIConsoleExample/bin/Release/

